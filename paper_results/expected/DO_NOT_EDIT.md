# Immutable pre-refactor gold

These files were frozen before any scientific-code refactoring.

Reproduction runs MUST write to `artifacts/`, never into this directory.
Later verification compares new outputs against these expected values.

Do not manually edit these files.
